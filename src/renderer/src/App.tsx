import Container from '@mui/material/Container';
import Editor from './Editor';

const App: React.FC = () => {
  return (
    <Container>
      <Editor />
    </Container>
  );
};

export default App;
